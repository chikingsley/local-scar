# Voice Agent Configuration
# Copy to .env and update values

# =============================================================================
# LLM (Ollama)
# =============================================================================
# Ollama server URL (use your machine's IP if running on another host)
OLLAMA_HOST=http://localhost:11434

# Model name (qwen3:8b recommended for tool calling + low latency)
OLLAMA_MODEL=qwen3:8b

# =============================================================================
# STT Configuration (Parakeet - local ONNX)
# =============================================================================
# Parakeet model (runs inside agent container)
STT_MODEL=nemo-parakeet-tdt-0.6b-v3

# Device for STT inference (cuda, cpu)
STT_DEVICE=cuda

# Optional: quantization for lower memory usage (leave empty for float32)
# STT_QUANTIZATION=int8

# =============================================================================
# TTS Configuration (Chatterbox)
# =============================================================================
# Chatterbox voice (or path to reference audio for cloning)
TTS_VOICE=default

# =============================================================================
# n8n MCP Integration (optional)
# =============================================================================
# n8n MCP endpoint URL (for workflow tools)
# N8N_MCP_URL=http://192.168.1.100:5678/mcp-server/http

# n8n MCP access token (get from n8n Settings > MCP Access)
# N8N_MCP_TOKEN=your_n8n_mcp_token_here

# =============================================================================
# Wake Word Detection (Picovoice Porcupine)
# =============================================================================
# Get your access key from https://console.picovoice.ai/
# This is optional - wake word only works if you provide a key
# PORCUPINE_ACCESS_KEY=your_key_here
